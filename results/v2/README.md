# results/v2 — v2 run results

**Status:** v2 (EfficientNet-B0 + SpecAugment + pos_weight + balanced sampler)
finished at **val macro ROC-AUC = 0.8401** — did NOT beat the v1 baseline (0.8529).

No metrics/summary files were saved locally. Re-download `metrics.json` and
`per_class_metrics.csv` from the Kaggle v2 run and place them here if needed.
