# models/v2 — v2 checkpoints

**Status:** empty — the v2 Kaggle run (val AUC 0.8401, did NOT beat v1) did not
produce a locally saved checkpoint.

If the v2 checkpoint is re-downloaded from Kaggle, place it here as `model.pth`.
