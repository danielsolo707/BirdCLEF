# models/v3 — v3 checkpoints

**Status:** empty — ready for the next v3 training run (4th-place-style recipe).

Put trained checkpoints here, e.g. `model_fold0_best.pth` (or promote the best
fold to `model.pth`) after the run beats the v1 baseline (val AUC 0.8529).
